[package]
name = "roc_desk_common"
version = "0.1.0"
edition = "2021"
license-file = "../../LICENSE"

[dependencies]
roc_desk_core = { path = "../core" }
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
